# Copy to .env and fill in. Do not commit .env.
# ─── Gemini (free with Pro / Google AI Studio) ───
# Get key: https://aistudio.google.com/app/apikey
# If set, Gemini is used for: answer check, live feedback, generate questions, generate path.
GEMINI_API_KEY=
# GEMINI_MODEL=gemini-2.5-flash

# ─── OpenAI (paid or free trial) ───
# Used if GEMINI_API_KEY is not set. Also required for "Draw your work" AI marking (vision).
OPENAI_API_KEY=
# OPENAI_API_BASE=https://api.openai.com
# OPENAI_MODEL=gpt-4o-mini
